# 编码规范

进行代码修改的时候需要同时确认对外暴露的接口语法及其语义等是否发生了变化.
你需要同步更新 C, C++, Go, Python, Java, Node 语言的暴露接口,
给予这些语言以和 Rust 自身等价的接口, 补到和 Rust 侧同等级的配置能力.

跨语言接口应该是非侵入式的, 源 crate 不应该为特定的跨语言接入而污染代码, 相关接入可以使用另一个 crate 实现.

修改代码的时候, 同步更新 README, 和所有暴露接口的文档.
